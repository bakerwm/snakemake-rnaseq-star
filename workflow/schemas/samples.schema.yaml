$schema: "http://json-schema.org/draft-06/schema#"

description: an entry in the sample sheet
properties:
  sample_name:
    type: string
    description: sample name/identifier
  group:
    type: integer
    description: group of wt-mut pair
  treatment_1:
    type: string
    description: wt or mut for sample_name in each group

required:
  - sample_name
